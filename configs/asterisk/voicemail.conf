[general]
format = wav49
serveremail = mvno@localhost
attach = no
maxmsg = 100
maxsecs = 300
minsecs = 3

[default]
; Demo mailbox: dial 8XXX -> VoicemailMain(mailbox XXX). Use 8100 -> mailbox 100,
; 8100 works because the dialplan passes ${EXTEN:1} (100). Mailbox 1000 (the
; screening "recorded-message reply" target) is the named demo box.
100 => 100,Demo Voicemail Box,testpass
1000 => 1000,Team Member,testpass
