# Live-Mic Re-Run — Real Voice vs Canned Speech (2026-08-06)

**Goal**: replace the invalid Phase-1 claim (RED FLAG 1: `live-caller.wav` was
byte-identical to `baresip-call.wav` — both the canned file) with a run where
the caller leg provably carries real, distinct human speech.

## Call metadata

- Call window (local, EEST): `19:58:02` INVITE -> `19:58:20` BYE (~18 s media)
- Fresh pcap: `state/spool/pcaps/d8e49c23af155154-a0d90f6f374aafa0.pcap`
- RTPEngine recorded the media; SIP flows in `journalctl` (baresip-tx/rx):
  - `INVITE sip:15559998888@10.89.0.23:5060` (from tx, 15553332211)
  - `SIP/2.0 200 Answering` (rx, `answermode=auto`)
  - `BYE sip:15559998888@10.89.0.60:34886` (hangup at 19:58:20)
- Audio legs (RTP, PCMU 8 kHz, even dstport split):
  - **tx -> rx (caller leg)**: src `10.89.0.61`, dstport 30000 — 894 pkts / 17.9 s
  - **rx -> tx (callee leg)**: src `10.89.0.60`, dstport 30086 — 117 pkts / 2.34 s
    (= speech8k.wav duration; rx `audio_source aufile,/media/speech8k.wav`)
- Extraction: `tshark -Y "udp.dstport==<port> and data.data" -e data.data`,
  then strip the 12-byte RTP header per packet (payload = 160 B/pkt).

> Lesson (2026-08-06): the packet-count heuristic in the guide can mislabel
> legs when the canned file is shorter than the call (117 pkts = 2.34 s file).
> Legs here are labeled by **source IP** (`10.89.0.61` = tx = caller voice,
> `10.89.0.60` = rx = canned), verified by packet counts and correlation.

## ASR transcripts (Vosk, 16 kHz, loudnorm I=-16:TP=-1.5:LRA=11)

Host spool transcript (`state/spool/archived/`):

- `live-caller.txt` (caller leg, real mic):
  `"okay this is yet fucked up testing that dmv in oh spam filter was my own voice when back"`
- `baresip-call.txt` (callee leg, canned):
  `"you have won a prime target now"`

**Assertion: transcripts differ** — caller leg contains distinct real speech
("spam filter", "my own voice", "call me back"); callee leg = the canned
phrase verbatim. `live-caller.txt != baresip-call.txt` PASS.

## AI filter verdicts (telecom-api NativeVoskService, 17:07 UTC)

```
17:07:14.220Z Native Java 21 Vosk ASR Transcribed [live-caller.wav]: {
  "text" : "okay this is yet fucked up testing that dmv in oh spam filter was my own voice when back"
}
17:07:14.222Z AI transcript verdict [live-caller]: allow=true, reason='Clean content'
17:07:15.115Z Native Java 21 Vosk ASR Transcribed [baresip-call.wav]: {
  "text" : "you have won a prime target now"
}
17:07:15.118Z AI transcript verdict [baresip-call]: allow=false, reason='Spam (phishing phrase detected)'
```

The spam filter allowed the real human voice (Clean content) and **blocked the
canned phishing phrase** (allow=false, Spam) — end-to-end, on the live call.

## Audio sanity (linear-PCM cross-correlation vs speech8k.wav)

- `caller leg vs canned`: xcorr ~ -0.001 (no match — real speech)
- `callee leg vs canned`: xcorr ~ 0.007 (no global match — note: aufile played
  the file once; xcorr done on whole 17.9 s leg vs 2.34 s file start)
- Durations: caller 17.9 s, callee 2.34 s, canned file 2.33 s
- RMS: caller 0.180, callee 0.235, canned 0.094 (both legs = live audio, not silence)

## Audible / speaker-proof recording

The real-voice caller leg was played through the **physical sink**
(`alsa_output.pci-0000_05_00.6.analog-stereo`) while the sink's **monitor
source** (`...analog-stereo.monitor`) was captured with `parec`:

- Playback: `aplay /tmp/opencode/live2/audible-play.wav` (live-caller leg, 48 kHz)
- Capture: `parec --raw --device=alsa_output.pci-0000_05_00.6.analog-stereo.monitor
  --rate 48000 --channels 2 --format s32le` -> `monitor-capture.raw` (20.1 s)
- Energy profile: silence (frames 0-20 = 2.0 s pre-roll), audio in the
  playback window frames 21-40 (RMS up to 0.113, peak 0.595) — the sink
  demonstrably emitted the audio, captured from its own monitor.
- **Monitor ASR (Vosk)**: `"okay this is yet fucked up getting the game v in a span"`
  — the same real utterance as the caller leg; **differs from the canned phrase**.

## Files

- `state/spool/pcaps/d8e49c23af155154-a0d90f6f374aafa0.pcap` — call recording
- `state/spool/archived/live-caller.txt|.wav`, `baresip-call.txt|.wav` — transcripts
- `state/spool/archived/speaker-proof.txt|.wav` — monitor capture + ASR
- `/tmp/opencode/live2/{micvoice,cannedleg}.ulaw` — extracted raw legs
- `/tmp/opencode/live2/monitor-capture.raw` — monitor capture (speaker proof)
