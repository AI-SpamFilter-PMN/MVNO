# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo

# Java / Maven
target/
*.jar
!mvnw
!mvnw.cmd

# State (runtime data)
state/
*.db-shm
*.db-wal
configs/kamailio/kamailio.db

# Vendor (generated by scripts/bootstrap.sh — too large for git)
vendor/

# IDE
.idea/
.vscode/
*.swp

# Docs — private personal guides & notes (strictly local-only)
docs/*.rar
docs/*.pdf
docs/*.drawio
docs/*.png
!docs/architecture_flow.png
!docs/ims_voice_call_flow.png
!docs/sms_interception_flow.png
docs/academic/

# NOTE: docs/implementation_guide.md is UNIGNORED (like API_CONTRACT.md) so it can
#       be shared with teammates on GitHub — keep the Goal 7 runbook section in sync.

# NOTE: docs/API_CONTRACT.md is UNIGNORED so it can be shared with teammates on GitHub!

# ============================================================
# !! SECURITY: AI AGENT WORKSPACE — NEVER PUBLISH !!
# ============================================================
# .agents/ contains local AI agent workspace rules (AGENTS.md)
# that are STRICTLY machine-local configuration.
# This folder MUST NEVER appear in any git commit, push,
# PR, or fork on ANY remote repository.
#
# History was already purged via git-filter-repo.
# This rule is the permanent guard against re-introduction.
# ============================================================
.agents/
.agents
AGENTS.md
**/AGENTS.md
.opencode/
.opencode
opencode.jsonc
opencode.json
# .cursorrules / .goosehints — agent rule files (Cursor, Goose).
# Same policy as AGENTS.md: STRICTLY machine-local, never publish.
.cursorrules
.goosehints
